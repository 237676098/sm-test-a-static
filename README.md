# sm-test-a-static

Public test repo for services_manager. Tiny static HTTP server on :8080.

- `/`        → "hello from sm-test-a-static"
- `/healthz` → "ok"

Purpose: verifies the happy path — public clone, build, run, Caddy route, health probe green.
